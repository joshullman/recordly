<p id="notice"><%= notice %></p>

<h1><%= @album.title %></h1>
<h3>by: <%= link_to @album.artist.name, user_artist_path(current_user, @album.artist) %></h3>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

<%= render partial: 'layouts/all_songs', locals: { user: current_user, songs: @songs } %>

</table>

<br> 

<%= link_to 'Edit', edit_user_album_path(current_user, @album) %> |
<%= link_to 'Back', user_albums_path(current_user) %>
