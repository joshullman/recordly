<h1>Editing Artist</h1>

<%= render partial: 'form', locals: {user: current_user, artist: @artist} %>

<%= link_to 'Show', user_artist_path(current_user, @artist) %> |
<%= link_to 'Back', user_artists_path(current_user) %>
